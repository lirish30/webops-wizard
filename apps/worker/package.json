{
  "name": "@webops-wizard/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --emitDeclarationOnly false",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@webops-wizard/config": "workspace:*",
    "@webops-wizard/db": "workspace:*",
    "@webops-wizard/integrations": "workspace:*",
    "@webops-wizard/types": "workspace:*",
    "bullmq": "^5.46.1",
    "ioredis": "^5.4.2",
    "zod": "^3.24.2"
  }
}
